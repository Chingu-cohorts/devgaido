createDimensions(steps)
  for step in steps
    .width-{step}
      width: step + '%' !important
    .height-{step}
      height: step + '%' !important

createDimensions($dimensionSteps)

.width-auto
  width: auto

.height-auto
  height: auto